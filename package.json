{
  "name": "roadguard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others-on-fail --silent \"npm run backend\" \"npm run frontend\"",
    "install": "cd frontend && npm install && cd ../backend && npm install && cd ..",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "create-admin": "cd backend && node createAdmin.js",
    "create-requests": "cd backend && node createSampleRequests.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
